export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});try{const{text=''}=req.body||{};if(!text.trim())return res.status(200).json({text:''});const apiKey=process.env.OPENAI_API_KEY;if(!apiKey)return res.status(200).json({text:localSoften(text)});const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${apiKey}`},body:JSON.stringify({model:'gpt-4.1-mini',input:[{role:'system',content:'거친 야구 팬 일기를 짧고 자연한 한국어 회고 문장으로 순화해. 길게 쓰지 마. 2문장 이내.'},{role:'user',content:text}],max_output_tokens:120})});const data=await response.json();const out=data.output_text||localSoften(text);return res.status(200).json({text:out})}catch{return res.status(200).json({text:localSoften(req.body?.text||'')})}}function localSoften(text){return String(text).replace(/개짜증|개빡침|개열받/gi,'많이 답답했다').replace(/미친/gi,'정말').replace(/개못/gi,'아쉬웠다')}