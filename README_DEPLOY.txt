배포 방법

1) 이 압축 파일 안의 모든 파일을 기존 프로젝트에 덮어쓰기
2) 반드시 api 폴더까지 GitHub에 같이 push 하기
3) Vercel 재배포
4) 배포 후 아래 주소가 열리는지 확인
   /api/kbo-standings

정상 응답이면 JSON이 보여야 합니다.
그 다음 사이트의 'KBO 순위표 보기' 페이지에서 실시간 순위가 뜹니다.
